class Solution:
    def getConcatenation(self, nums: List[int]) -> List[int]:
        ans = []

        for n in range(len(nums)):
            ans.append(nums[n])
        
        for n in range(len(nums)):
            ans.append(nums[n])


        return ans


        
